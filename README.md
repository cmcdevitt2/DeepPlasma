## Installation

To clone this repository and its submodules (DeepRunAway), use the `--recursive` flag:

```bash
git clone --recursive https://github.com/cmcdevitt2/DeepPlasma.git
```

When pulling, to obtain updates on all submodules use:

```bash
git pull --recurse-submodules
```